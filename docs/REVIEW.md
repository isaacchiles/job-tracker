## Design Document Review: JobTracker

### Summary
All issues from prior rounds have been addressed. The two round-2 issues (Store API surface naming/signature drift with import/merge, and PR 2 scope expansion from front-loaded safety logic) have been fully resolved by the writer with precise, minimal edits only to the affected sections of the design document.

Verification of the two most recent fixes (as requested):
- **Store API alignment + public vs internal**: All references across State & Persistence Layer, Import/Export Safety and UX subsection (including always-export-first, merge semantics, PR2 implements note, prepareForImport), data flow Mermaid, PR 2 description, and the Store API excerpt itself now consistently use *only* the exact public `AppStore` signatures (e.g. `importData(data: AppData, mode: 'replace' | 'merge'): ImportResult;` taking the incoming payload + mode as the entrypoint). The new standalone "**Public API vs internals**" paragraph (placed immediately after the interface) clearly distinguishes the stable public contract in `store.ts` (what PR2 must implement and expose) from private helpers (e.g. `mergeData` calculator, `prepareForImport`, `computeDeleteSummary`, dupe checker in persistence/utils). ImportResult is now fully fleshed out with concrete fields (no placeholders). All prior conflicting prose (old `importData(current, incoming, mode)`, loose "mergeByIdWithUpdatedAt", etc.) has been eliminated. These changes are clean, unambiguous for implementers, and introduce no conflicts with the 7 earlier fixes (e.g., DeleteCompanySummary, uniqueness soft-check returns, Delete Semantics invariants, and full import wizard flow remain correctly cross-referenced and intact).
- **PR2 scope/strategy notes**: PR 2 title now includes "(foundational safety surface)". The description explicitly acknowledges it as "the heaviest foundational PR due to front-loaded data safety requirements from Delete Semantics, Import/Export Safety, Store API surface, and uniqueness policy", provides the suggested implementer guidance ("break into logical commits within this PR (pure utils first: merge fn, deleteSummary calculator, dupe checker + isolated tests; then store actions + invariants + public surface; then LS+import/export primitives)"), and reaffirms the post-PR2 runnable foundation ("via dev console or minimal test harness calling store methods + persist roundtrips for import/delete/export"). "Overall Strategy Notes" (and cross-refs in PR descriptions) now explicitly call out the expansion cause, confirm "no sequencing change", state that "PR2 ... delivers a runnable console/test-harness foundation for *all* later slices (public store methods + full persist/import/export/delete roundtrips available immediately after it)", add "For PR2 specifically: test the full public surface + private utils in isolation.", and note "Later PRs (PR3 delete UI, PR7 import wizard) only consume the public Store API surface implemented in PR2". These updates are consistent with the original incremental/vertical-slice intent, do not alter PR ordering/deps/non-goals, do not conflict with prior safety additions (they reference them positively), and keep the plan realistic for solo development while preserving the "specific enough to implement directly" bar.

No new problems, regressions, inconsistencies, or scope changes were introduced by these fixes (verified via targeted greps and section reads of Store API, Import/Export, PR 2, Strategy Notes, data model cross-refs for Delete Semantics/uniqueness/notes field, and overall structure). All 9 issues total are closed. The design document remains concise, technically sound, complete, and ready for direct implementation starting with PR 1. Workspace at /Users/isaac/Documents/job-tracker remains fresh (only README.md + .git; no source code or other files).

### Final Verdict
0 open issues of any severity. The design is approved and ready for implementation. The design document is at: /tmp/grok-design-doc-fd1440c1.md

### Summary of Review Rounds
- Initial issues: 7 (addressed)
- Round 2 issues: 2 (addressed)
- Total addressed: 9
- Open issues now: 0

### Strengths (carried forward, with final notes)
- Extremely concrete data model section: full exact TS interfaces + Zod-ready enums (including Opportunity notes), embedded vs linked relationships, explicit via_company/contracting modeling, Delete Semantics (with primary/via distinction, summary type, invariants, and cleanup), next-action derivation, headcount bucketing, company soft-uniqueness policy, and realistic full AppData JSON example (with notes). Directly enables copy-paste into PR1 types.ts and fully addresses all prior clarifications and ambiguities.
- Thorough "Alternatives Considered" (7 detailed options) and expanded "Key Decisions" (12 items, including the company duplicates soft-warning with rationale) that justify choices specifically for solo/client-side/small-volume local app + contracting use cases.
- Strong, realistic PR plan: 8 vertical-ish slices with explicit per-PR affected files, deps, and strategy notes. Early PRs still deliver runnable vertical slices (e.g. Companies after PR3 independently useful with delete/uniqueness wired); persistence front-loaded so safety features (import wizard primitives, delete semantics) are available early. Test guidance examples cover the new delete/import scenarios. PR2 now has clear internal breakdown and "runnable foundation" expectations post the round-2 clarifications.
- Excellent usability and daily-driver focus: overdue/due-soon visuals, AI-native + via badges, promoted next-action, stage colors, auto-applied_at, inline adds, keyboard, last-saved, optimistic DND. The safety flows (always-export-first import, detailed delete confirm with summary) further improve daily reliability without adding friction.
- Outstanding risk and non-functional coverage: explicit non-goals, performance targets, "never lose current data" now backed by concrete mechanisms (Zod pre-mutation, pre-backup export, cleanup passes, corrupt handling, FS graceful + caveats with deferral), debounced persist, seed guidance, accessibility. The new subsections (Delete Semantics, Import/Export Safety wizard + merge, FS caveats, Store API excerpt + Public vs internals) make "data loss on import" and "browser compatibility for FS API" risks fully mitigated in spec.
- Mermaid diagrams + supporting notes: ERD now uses correct logical ".." notation with "Diagram note" on validation/approximations; UI hierarchy completed; data flow updated with consistent labels and safety paths + validation note. All three are more production-useful.
- The full set of revisions (9 issues addressed across rounds) added excellent concrete safety mechanisms (full Delete Semantics, Import/Export Safety wizard + merge, FS caveats, Store API excerpt with public/internals clarity) that make the design more robust and directly implementable for a daily job-seeker tool, without bloating scope, altering MVP cut, or impacting feasibility for a solo local-first build.

**End of review notes.**